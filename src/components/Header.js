import React from 'react';

const Header = () => {
    return (
        <header className={'header'}>
            <div className="container">
                Тестовое задание
            </div>
        </header>
    );
};

export default Header;